describe('CurrentUser Decorator', () => {
  it('should be defined', () => {
    // The decorator is a parameter decorator created with createParamDecorator
    // It's tested through integration tests
    expect(true).toBe(true);
  });
});
